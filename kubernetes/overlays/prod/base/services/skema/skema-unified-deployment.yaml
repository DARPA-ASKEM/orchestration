---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: skema-unified
spec:
  strategy:
    type: RollingUpdate
  template:
    spec:
      tolerations:
        - key: 'workload'
          operator: 'Equal'
          value: 'skema-text-reading'
          effect: 'NoSchedule'

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: skema-rs
spec:
  strategy:
    type: RollingUpdate
  template:
    spec:
      tolerations:
        - key: 'workload'
          operator: 'Equal'
          value: 'skema-text-reading'
          effect: 'NoSchedule'

apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-keycloak
spec:
  template:
    spec:
      containers:
        - name: gateway-keycloak
          env:
            - name: KC_HOSTNAME
              value: app.terarium.ai
                  